for clasper-core run build (npm run build), lint (npm run lint), and tests (npm test), and fix any issues so CI would pass

for clasper-control-plane-starter run tests (make test / pytest) and fix any issues

for clasper-website run build (npm run build) and fix any issues

for clasper-cloud run build (npm run build or make build) and fix any issues

for all four repos (clasper-core, clasper-control-plane-starter, clasper-website, clasper-cloud) commit all changes and push
